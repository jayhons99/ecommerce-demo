import { HeroHome, Contact, FeaturedProducts, Services } from "../components";

const HomePage = () => {
  return (
    <main>
      <HeroHome />
      <FeaturedProducts />
      <Services />
      <Contact />
    </main>
  );
};
export default HomePage;
